<html>
    <head>
        <link rel="stylesheet" href="/css/image.css">
    </head>
    <title>
        {{ .title }}
    </title>
    <body>
        <img src="{{.image.SourcePath}}">
    </body>
</html>